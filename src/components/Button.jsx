import "../style/Button.css";

export const Button = ({}) => {
    return (
        <div className="button-container button-pd">
            <p className="p-regular bold">Events</p>
        </div>
    );
};
